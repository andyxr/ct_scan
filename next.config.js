/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the Next.js dev tools indicator (the logo badge in the corner).
  devIndicators: false,
}

module.exports = nextConfig